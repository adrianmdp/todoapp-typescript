import React, { FC } from "react";
import { Layout, Main } from "@components";

const App: FC = () => {
	return (
		<Layout>
			<Main title="Título de prueba" />
			Hola Mundo
		</Layout>
	);
};

export default App;
