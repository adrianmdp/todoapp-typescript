import React, { FC } from "react";
import { Layout, Main } from "@components";

const App: FC = () => {
  return (
    <Layout>
      <Main title="Título de prueba" />
    </Layout>
  );
};

export default App;
